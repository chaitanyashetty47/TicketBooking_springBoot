spring:
  data:
    mongodb:
      uri: mongodb+srv://Aditya:root@cluster0.cjcxybq.mongodb.net/?retryWrites=true&w=majority
      database: test

  datasource:
    url: jdbc:postgresql://localhost:5432/jwt_security
    username: postgres
    password: root
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
        ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect
